// 占位模板 — 共享 .mcp.json（agent team teammate 形态）
// 规则见 ../design-mcp-config-shape.md。
//
// teammate 形态要点：
//   - MCP server 由**官方 Claude Code plugin 自行注册**（GitHub Plugin / Atlassian Plugin），
//     不落此 .mcp.json。用户通过 /plugin install + /mcp OAuth 授权完成配置。
//   - 本文件保留为项目级 .mcp.json 骨架；如需项目级 MCP 配置（如本地调试 server），
//     可在此添加。dm-seek 核心溯源不依赖本文件中的 MCP server 条目。
//   - 独占靠各 agent 的 tools 白名单（见 ./agent-tools-allowlist.placeholder.md），
//     官方 plugin 注册的 mcp__github__* / mcp__atlassian__* 工具同样受白名单管辖。
//   - 不走 agent frontmatter 内联——teammate 形态下 frontmatter 的 mcpServers 被忽略。
{
  "mcpServers": {
    // --- 占位：dm-seek 核心溯源使用官方 Claude Code plugin ---
    // GitHub Plugin：/plugin install github → /mcp OAuth 授权
    //   server 名: github  工具前缀: mcp__github__*
    // Atlassian Plugin：/plugin install atlassian → /mcp OAuth 授权
    //   server 名: atlassian  工具前缀: mcp__atlassian__*
    //
    // 官方 plugin 自行注册其 MCP server，不在此添加条目。
    // 如需本地调试或额外 MCP server，在下方按需添加。
  }
}
